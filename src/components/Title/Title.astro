---
import './title.module.scss';

interface TittleProps {
	title: string;
	subTitle: string;
}

const { title, subTitle }: TittleProps = Astro.props as TittleProps;
---

<h1>{ title }</h1>
<h3>{ subTitle }</h3>
